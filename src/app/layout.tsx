import type { Metadata } from "next";
import { Khula } from "next/font/google";
import { Providers } from "@/context/Providers";
import "./globals.css";
import { Suspense } from "react";
import Loading from "./loading";

const inter = Khula({
  weight: "400",
  style: "normal",
  display: "swap",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Wine Portal",
  description: "Generated by Blazar Labs",
  openGraph: {
    title: "Blazar Labs Wine Portal",
    description:
      "Tokenization and supply chain tracking of wines on the Cardano blockchain",
    url: "https://wines.blazarlabs.io/",
    siteName: "Blazarlabs Wine Portal",
    images: [
      {
        url: "https://wines.blazarlabs.io/og.jpg",
        width: 1080,
        height: 618,
      },
    ],
    locale: "en-US",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Blazar Labs Wine Portal",
    description:
      "Tokenization and supply chain tracking of wines on the Cardano blockchain",
    images: ["https://wines.blazarlabs.io/og.jpg"],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          <Suspense fallback={<Loading />}>{children}</Suspense>
        </Providers>
      </body>
    </html>
  );
}
